{
  "name": "@ai-governance/worker",
  "version": "0.1.0",
  "description": "Background worker for approval timeouts, evidence aggregation, notifications",
  "type": "module",
  "main": "./dist/worker.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/worker.js",
    "dev": "node --watch dist/worker.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@ai-governance/shared": "file:../shared",
    "@aws-sdk/client-s3": "^3.0.0",
    "dotenv": "^16.4.5",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "~5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
