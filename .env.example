# PostgreSQL connection. Use either DATABASE_URL or the PG_* variables.
# For non-default port (e.g. 5434): use that port in the URL or set PG_PORT=5434
# DATABASE_URL=postgresql://user:password@localhost:5432/ai_governance
# DATABASE_URL=postgresql://user:password@localhost:5434/ai_governance

# PG_HOST=localhost
# PG_PORT=5432
# PG_PORT=5434
# PG_USER=postgres
# PG_PASSWORD=your_password_here
# PG_DATABASE=ai_governance

# Required: If your Postgres uses SCRAM-SHA-256 auth, set PG_PASSWORD to your user's password.
# Optional: connection pool size (default 10)
# PG_POOL_SIZE=10

# API service (packages/api). Use 3001 if admin (Next.js) runs on 3000.
# PORT=3001
# HOST=0.0.0.0
# LOG_LEVEL=info
# CORS_ORIGIN=https://admin.example.com

# Authentication: JWT secrets for agent and optional user/session tokens
# AGENT_JWT_SECRET=your-secret-for-signing-agent-keys
# USER_JWT_SECRET=your-secret-for-session-tokens
# JWT_SECRET=fallback-if-above-not-set

# Sentiment validation check: API that accepts POST { "text": "..." } and returns { "sentiment_score": number } (-1 to 1)
# SENTIMENT_API_URL=https://your-sentiment-service.example.com/analyze
# SENTIMENT_API_KEY=optional-bearer-token

# S3-compatible evidence storage (optional)
# S3_BUCKET=ai-governance-evidence
# S3_REGION=us-east-1
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=

# OpenTelemetry (optional; set OTEL_SDK_DISABLED=true to disable)
# OTEL_SERVICE_NAME=ai-governance-api
# OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://localhost:4318/v1/metrics

# Worker (packages/worker). Health server port (default 3099 to avoid conflict with API on 3001)
# WORKER_QUEUE=default
# WORKER_CONCURRENCY=2
# WORKER_HEALTH_PORT=3099
# WORKER_ID=worker-1
